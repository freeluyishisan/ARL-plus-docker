# 复制为 .env 后，普通 docker compose up -d 会自动启动 MCP
COMPOSE_PROFILES=mcp

# ARL MCP 配置
ARL_TOKEN=change-me
ARL_ALLOWED_SUFFIXES=example.com
ARL_BASE_URL=https://web:443
ARL_VERIFY_TLS=false
ARL_TIMEOUT=30

# MCP 服务配置
MCP_TRANSPORT=sse
MCP_DATA_DIR=/data
MCP_MAX_PAGE_SIZE=500
MCP_DEFAULT_MAX_PAGES=10
